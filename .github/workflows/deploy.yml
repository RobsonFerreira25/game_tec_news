name: Deploy to Hostinger

on:
  push:
    branches:
      - main # Garanta que este é o nome da sua branch principal

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Checkout do Código
        uses: actions/checkout@v4

      - name: 🛠️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-size: 20
          cache: 'npm'
          cache-dependency-path: './game_tec_news/package-lock.json'

      - name: 📦 Instalar Dependências
        run: |
          cd game_tec_news
          npm install

      - name: 🏗️ Build do Projeto
        run: |
          cd game_tec_news
          npm run build

      - name: 🚀 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          local-dir: ./game_tec_news/dist/ 
          server-dir: /home/u619424515/domains/gametecnews.com/public_html # Ajuste se o seu site estiver em uma subpasta
          dangerous-clean-slate: false # Define como true se quiser apagar arquivos antigos que não estão no dist
